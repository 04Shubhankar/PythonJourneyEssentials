import logging

# Configure basic logging setup with timestamp
logging.basicConfig(format='%(asctime)s - %(message)s', level=logging.INFO)

# Log a message with a timestamp
logging.info('Admin logged in')
